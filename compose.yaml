services:
  app:
    build: .
    image: xtream-viewer:latest
    container_name: xtream-viewer
    ports:
      - "5173:5173"
    environment:
      - PORT=5173
      - NODE_ENV=production
    restart: unless-stopped
